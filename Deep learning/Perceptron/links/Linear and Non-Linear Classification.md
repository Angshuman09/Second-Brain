**Linear Classification:**  
In linear classification, the model separates different classes using a **straight line** (in 2D), a **plane** (in 3D), or a **hyperplane** (in higher dimensions).

**Non-Linear Classification:**  
In non-linear classification, the data cannot be separated by a single straight line, so the model uses **curved or more complex boundaries** to divide the classes.